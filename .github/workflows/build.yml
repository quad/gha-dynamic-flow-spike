name: toplevel build
on: push

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - run: gh workflow run sub-ok.yml --ref ${{ github.ref }}
      - run: gh workflow run sub-fail.yml --ref ${{ github.ref }}